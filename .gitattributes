# Use LF line endings in the repo and working tree on every platform.
* text=auto eol=lf

# Keep fixture files byte-for-byte as fetched from ENTSO-E.
tests/fixtures/** -text

*.gz binary
*.parquet binary
